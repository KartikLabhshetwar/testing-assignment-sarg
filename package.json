{
  "name": "testing-assignment-sarg",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "cd apps/web && pnpm run dev",
    "build": "cd apps/web && pnpm run db:generate && pnpm run build",
    "start": "cd apps/web && pnpm run start",
    "db:push": "cd apps/web && pnpm run db:push",
    "db:studio": "cd apps/web && pnpm run db:studio",
    "db:generate": "cd apps/web && pnpm run db:generate",
    "db:migrate": "cd apps/web && pnpm run db:migrate",
    "db:seed": "cd apps/web && pnpm run db:seed",
    "postinstall": "cd apps/web && pnpm run db:generate"
  },
  "dependencies": {},
  "devDependencies": {
    "turbo": "^2.5.4"
  },
  "packageManager": "pnpm@10.17.1"
}
